const routes = {
  home: '/',
  tasks: '/projects/:projectId/:sprintid',
  sprints: '/projects/:projectId',
  projects: '/projects',
  signup: '/register',
  login: '/login',
};
export default routes;
