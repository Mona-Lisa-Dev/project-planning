import axios from 'axios';

const getAllProjects = () => async dispatch => {
  try {
  } catch (error) {}
};

const createProject = () => async dispatch => {
  try {
  } catch (error) {}
};

const deleteProject = () => async dispatch => {
  try {
  } catch (error) {}
};

const updateProjectName = () => async dispatch => {
  try {
  } catch (error) {}
};

const addParticipant = () => async dispatch => {
  try {
  } catch (error) {}
};

/* eslint import/no-anonymous-default-export: [2, {"allowObject": true}] */
export default {
  getAllProjects,
  createProject,
  deleteProject,
  updateProjectName,
  addParticipant,
};
